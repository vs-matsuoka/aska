import { Story, Meta } from '@storybook/react';
import AboutPage from '.';

export default {
  title: 'AboutPage',
  component: AboutPage
} as Meta;

export const Default: Story = () => <AboutPage />;
