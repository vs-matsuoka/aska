import { Story, Meta } from '@storybook/react';
import IndexPage from '.';

export default {
  title: 'IndexPage',
  component: IndexPage
} as Meta;

export const Default: Story = () => <IndexPage />;
