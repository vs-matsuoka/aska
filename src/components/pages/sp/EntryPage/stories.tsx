import { Story, Meta } from '@storybook/react';
import EntryPage from '.';

export default {
  title: 'EntryPage',
  component: EntryPage
} as Meta;

export const Default: Story = () => <EntryPage />;
